[package]
name = "bevy-52-card-pickup"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
bevy = { version = "0.18.0" }
rand = "0.10.0"

[target.wasm32-unknown-unknown.dependencies]
# spell-checker: disable-next-line 
getrandom = { version = "0.4.1", features = ["wasm_js"] }
